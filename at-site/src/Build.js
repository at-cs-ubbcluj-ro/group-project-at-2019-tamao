import React from "react";
import './App.css';

function Build(props) {
    return (
        <div className = "Build">
            <h2>{props.name}</h2>

        </div>
    );
}

export default Build;